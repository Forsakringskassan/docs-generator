# Integration test: minimal

This integration test contains a minimal project with only a few limited files.
No processors, extra assets, etc.

- The build should succeed.
- Tests in [`cypress/e2e/integration-tests/minimal.cy.ts`](../../cypress/e2e/integration-tests/minimal.cy.ts) should succeed.
