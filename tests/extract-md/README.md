# Integration test: extract markdown

This integration test contains a project using the `extractMarkdownProcessor()` to bundle documentation to a package.

- The build should succeed.
- Unit tests should succeed.
