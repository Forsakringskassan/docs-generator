import { type SourceFiles } from "./file-reader";

/**
 * Tiny wrapper to provide type completion for IDE.
 *
 * @public
 */
/* istanbul ignore next: not needed for coverage */
export function defineSources(sources: SourceFiles[]): SourceFiles[] {
    return sources;
}
